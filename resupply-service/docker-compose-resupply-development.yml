version: "3.9"
services:
  resupply-service-development:
    build:
      context: .
      target: docker-development
    ports:
      - ${APP_PORT}:${APP_PORT}             # APP_PORT from the file .env
      - '9229:9229'
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    env_file:
      - .env
    command: npm run start:dev
    depends_on:
      - mongo_db_resupply_development
    networks:
      - soa-team-e-network-development

  mongo_db_resupply_development:
    image: "mongo:4.4.15"
    restart: always
    ports:
      - ${MONGODB_PORT}:${MONGODB_PORT}
    networks:
      - soa-team-e-network-development

networks:
  soa-team-e-network-development:
    driver: bridge
